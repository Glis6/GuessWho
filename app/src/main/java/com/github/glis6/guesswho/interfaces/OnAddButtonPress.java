package com.github.glis6.guesswho.interfaces;
/**
 * An interface that shows that we support the add button press.
 *
 * @author Glis
 */
public interface OnAddButtonPress {
    /**
     * Gets called when the add button is pressed.
     */
    void onPress();
}
